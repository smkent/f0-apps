diff --git i/scripts/fbt_tools/fbt_apps.py w/scripts/fbt_tools/fbt_apps.py
index 3a17a79a..57db4868 100644
--- i/scripts/fbt_tools/fbt_apps.py
+++ w/scripts/fbt_tools/fbt_apps.py
@@ -1,3 +1,5 @@
+import os
+
 from ansi.color import fg
 from fbt.appmanifest import (
     AppManager,
@@ -125,6 +127,9 @@ def LoadAppManifest(env, entry):
 
         app_manifest_file_path = manifest_glob[0].rfile().abspath
         env["APPMGR"].load_manifest(app_manifest_file_path, entry)
+        env.Append(
+            LINT_SOURCES=[env.Dir(os.path.dirname(app_manifest_file_path))]
+        )
     except FlipperManifestException as e:
         if not GetOption("silent"):
             warn(WarningOnByDefault, str(e))
